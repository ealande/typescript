let isRaining: boolean = false;

isRaining = true;

let age: number = 27;

let height: number = 1.75;

const nationality: string = 'Brazilian';

const people: string[] = ['lucas', 'fernando'];

const tecnology: Array<string> = ['html', 'css', 'js'];

const grades: ReadonlyArray<number> = [7,8,9,10];

const list: [name: string, isStudying:boolean, age: number] = ['gian', true, 27]

let anaAge: number | string = 25;
anaAge: 'twenty five years';


let apiData: any;

apiData: 10;
apiData: true;
apiData: [1,2,3];
apiData: 'string';

let course = 'front end';

